# Screenshots

Captured from the running system, not mocked.

| File | What it shows |
|---|---|
| `01-landing.jpg` | Landing page. The hero is a live WebGL particle field — twenty thousand points that form the ₿ and then re-form as a fingerprint as you scroll. |
| `02-explorer-overview.jpg` | Explorer overview: entities under review, how many are high risk, how many attributed to an origin host, and laundering chains recovered. |
| `03-alert-explainability.jpg` | An alert opened. SHAP decomposition of the risk score, the laundering structure behind it, and whether the unsupervised model agreed. |
| `04-money-flow-graph.jpg` | Money flow and network sightings for one entity. Amber nodes are wallet entities, red are origin hosts; blue edges are money, red are first-relay sightings. |
| `05-pipeline-run.jpg` | The Pipeline page. All six stages can be run live from here against the real repository. |

Nothing here contains credentials or personal data. Every address, IP and ASN is
synthetic — the addresses come from the project's own generator and the network
addresses are RFC 5737 documentation ranges, which are reserved and route
nowhere.
